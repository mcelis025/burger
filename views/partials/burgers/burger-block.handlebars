<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}EAT!{{else}}WAKE UP!{{/if}}
	</button>

{{!-- 	<button class="delete" data-id="{{id}}">
		DELETE!
	</button> --}}

</li>